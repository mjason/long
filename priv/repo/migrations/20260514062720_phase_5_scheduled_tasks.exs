defmodule Long.Repo.Migrations.Phase5ScheduledTasks do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_sqlite.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:agent_scheduled_tasks, primary_key: false) do
      add :session_id,
          references(:agent_sessions,
            column: :id,
            name: "agent_scheduled_tasks_session_id_fkey",
            type: :uuid,
            on_delete: :delete_all
          ), null: false

      add :updated_at, :utc_datetime_usec, null: false
      add :inserted_at, :utc_datetime_usec, null: false
      add :metadata, :map, default: %{}
      add :next_run_at, :utc_datetime
      add :last_run_at, :utc_datetime
      add :enabled, :boolean, null: false
      add :max_delay_hours, :bigint
      add :every_n, :bigint
      add :schedule_time, :text
      add :repeat, :text, null: false
      add :prompt, :text, null: false
      add :name, :text, null: false
      add :id, :uuid, null: false, primary_key: true
    end

    create unique_index(:agent_scheduled_tasks, [:name], name: "agent_scheduled_tasks_name_index")
  end

  def down do
    drop_if_exists unique_index(:agent_scheduled_tasks, [:name],
                     name: "agent_scheduled_tasks_name_index"
                   )

    raise "SQLite does not support dropping foreign key constraints. " <>
            "You will need to manually recreate the `agent_scheduled_tasks` table without the `agent_scheduled_tasks_session_id_fkey` constraint. " <>
            "See https://www.techonthenet.com/sqlite/foreign_keys/drop.php for guidance."

    drop table(:agent_scheduled_tasks)
  end
end